# Toshik Babe Engine 🐾

Toshik Babe Engine — это современный, быстрый и приватный ИИ-ассистент, построенный на принципе local-first. 

## Технологический стек
- **Backend:** Bun + TypeScript (нативная производительность).
- **Frontend:** Tauri 2.x + React + shadcn/ui.
- **Мозг:** Google Gemini AI (через API).
- **Связь:** WebSocket (стриминг токенов в реальном времени).

---

## 🛠 Установка и запуск (локально)

Следуйте этим шагам, чтобы развернуть проект на своем компьютере.

### 1. Предварительные требования
Убедитесь, что у вас установлены:
- **[Bun](https://bun.sh/)** (основной рантайм и пакетный менеджер).
- **[Rust](https://www.rust-lang.org/tools/install)** (необходим для сборки Tauri-приложения).
- Системные зависимости для Tauri (если вы на Linux, проверьте [руководство Tauri](https://tauri.app/v1/guides/getting-started/prerequisites/)).

### 2. Клонирование репозитория
```bash
git clone https://github.com/ts-vit/toshik-babe.git
cd toshik-babe
```

### 3. Установка зависимостей
Из корня проекта выполните:
```bash
bun install
```

### 4. Настройка API ключа
Для работы «мозгов» Тошика нужен ключ Google Gemini. 
Создайте файл `.env` в корне проекта или экспортируйте переменную:
```bash
export GOOGLE_GENAI_API_KEY="ВАШ_КЛЮЧ_ЗДЕСЬ"
```
*(Получить ключ можно на [Google AI Studio](https://aistudio.google.com/))*

### 5. Запуск в режиме разработки

Вы можете запустить всё одной командой:
```bash
bun run dev
```
Это запустит:
- **Бэкенд** на порту 3001 (WebSocket сервер).
- **Фронтенд** (Tauri окно или веб-интерфейс на порту 1420).

**Раздельный запуск:**
Если хотите запустить только бэкенд или фронтенд:
```bash
bun run dev:backend   # Только сервер
bun run dev:frontend  # Только интерфейс
```

### 6. Сборка (Production)
Чтобы собрать готовое нативное приложение (.exe, .app или .deb):
```bash
bun run build
```
Готовые файлы появятся в `packages/frontend/src-tauri/target/release/bundle/`.

---

## 📂 Структура проекта
- `packages/backend`: Сервер на Bun, работа с Gemini API.
- `packages/frontend`: Интерфейс на React и конфигурация Tauri.
- `packages/shared`: Общие типы и логика для фронтенда и бэкенда.

## 🐾 Команды обслуживания
- `bun run typecheck` — Проверка типов во всем монорепозитории.
- `bun run lint` — Запуск линтера (ESLint).
- `bun run format` — Красивое форматирование кода (Prettier).

---
*Сделано с любовью и виляющим хвостиком Тошика!* 🐶
